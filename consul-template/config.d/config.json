# PASS
# ENV CONSUL_TOKEN
# ENV VAULT_TOKEN
# To process

consul = "consul.service.consul:8500"
retry = "10s"
log_level = "err"
vault {
  address = "http://vault.service.consul:8200"
  renew = false
}