<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
   "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <title>HHS VOC Tool Application</title>

    <link href="<%= "#{request.protocol}#{ENV.fetch('APP_ADMIN_HOST_PORT')}/assets/application.css" %>" media="screen" rel="stylesheet" />
    <link href="<%= "#{request.protocol}#{ENV.fetch('APP_ADMIN_HOST_PORT')}/assets/simplemodal.css" %>" media="screen" rel="stylesheet" />
    <link href="<%= "#{request.protocol}#{ENV.fetch('APP_ADMIN_HOST_PORT')}/assets/jquery-ui.min.css" %>" media="screen" rel="stylesheet" />
    <link href="<%= "#{request.protocol}#{ENV.fetch('APP_ADMIN_HOST_PORT')}/assets/pdf.css" %>" media="screen" rel="stylesheet" />
    <%= yield :head %>
  </head>
  <body>
    <div id="container">
      <div id="content">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
