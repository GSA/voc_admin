<% if !current_user.blank? %>
	<p class="nav"><%= link_to "Home", surveys_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Create New Survey", new_survey_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "View Responses", survey_responses_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Manage Images", images_url, :class => "nav"%></p>
	<p class="nav"><%= link_to "Manage Users", users_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Logout", logout_url, :class => "nav" %></p>
<% end %>
