<% if !current_user.blank? %>
	<p class="navTitle">Menu</p>
	<p class="nav"><%= link_to "View All Surveys", surveys_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Create New Survey", new_survey_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Survey Responses", survey_responses_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Categories", categories_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Users", users_url, :class => "nav" %></p>
	<p class="nav"><%= link_to "Logout", logout_url, :class => "nav" %></p>
<% end %>
