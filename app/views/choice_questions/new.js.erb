$("#edit_modal").html("<%= escape_javascript(render(:partial => 'new_choice_question', :locals => {:survey_version => @survey_version, :survey => @survey_version.survey, :choice_question => @choice_question})) %>").
	modal({autoResize:true, maxHeight:'100%', minWidth:'400px', maxWidth:'500px'});