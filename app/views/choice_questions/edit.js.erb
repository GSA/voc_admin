$("#edit_modal").html("<%= escape_javascript(render(:partial => 'edit_choice_question', :locals => {:survey_version => @survey_version, :survey => @survey_version.survey, :choice_question => @choice_question})) %>").
	modal({autoResize:true, maxHeight:'90%', minWidth:'330px', maxWidth:'500px'});