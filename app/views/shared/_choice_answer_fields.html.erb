<p class="answer_fields">
	<%= f.label :answer %>
	<%= f.text_field :answer %>
	<%= f.hidden_field :_destroy %>
	<%= link_to_function "Remove Answer", "remove_fields(this)" %>
	<br/>
	
  <%= f.label :next_page_id, "Go To Page: " %><%= f.collection_select :next_page_id, survey.newest_version.pages, :id, :number, {},  :class => "next_page_select" %>
</p>