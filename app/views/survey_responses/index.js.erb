$("#survey_response_list").html("<%= escape_javascript( render(:partial => 'survey_response_list', :locals => {:objects => @survey_responses, :version_id => @survey_version}) )%>");
$(".best_in_place").best_in_place();
  