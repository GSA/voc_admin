<h2>Survey Responses</h2>

<% for response in @survey_responses %>
  <%= response.survey_version.survey.name %>
	<%= link_to response.client_id[0..10], raw_responses_path(:client_id => response.client_id) %>
	<br/>
<% end -%>